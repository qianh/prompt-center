# Prompt Center 脚本使用指南

## 🚀 快速开始

### 一键启动前后端
```bash
./scripts/start.sh
```

### 停止所有服务
```bash
./scripts/stop.sh
```

### 查看服务状态
```bash
./scripts/status.sh
```

### 重启所有服务
```bash
./scripts/restart.sh
```

## 🎛️ 独立控制

### 前端服务

**启动前端：**
```bash
./scripts/start-frontend.sh
```

**停止前端：**
```bash
./scripts/stop.sh --frontend-only
```

**重启前端：**
```bash
./scripts/restart.sh --frontend-only
```

### 后端服务

**启动后端：**
```bash
./scripts/start-backend.sh
```

**停止后端：**
```bash
./scripts/stop.sh --backend-only
```

**重启后端：**
```bash
./scripts/restart.sh --backend-only
```

## 📊 状态检查

运行 `./scripts/status.sh` 会显示：

- **进程状态** - PID、运行状态
- **端口占用** - 端口3000和8000的使用情况
- **服务响应** - HTTP响应检查
- **日志信息** - 日志文件大小和位置
- **访问地址** - 当前可用的服务地址

## 📝 日志管理

### 查看实时日志
```bash
# 前端日志
tail -f logs/frontend.log

# 后端日志
tail -f logs/backend.log
```

### 日志文件位置
- 前端日志：`logs/frontend.log`
- 后端日志：`logs/backend.log`
- PID文件：`.frontend.pid`、`.backend.pid`

## 🔧 故障排除

### 端口被占用
如果端口被其他进程占用，脚本会自动清理：
```bash
# 手动清理端口
lsof -ti:3000 | xargs kill -9  # 前端端口
lsof -ti:8000 | xargs kill -9  # 后端端口
```

### 服务无法启动
1. 检查依赖是否安装：
   ```bash
   # 检查 uv
   uv --version
   
   # 检查 yarn
   yarn --version
   ```

2. 查看详细日志：
   ```bash
   tail -f logs/backend.log   # 后端启动日志
   tail -f logs/frontend.log  # 前端启动日志
   ```

3. 重新安装依赖：
   ```bash
   # 后端依赖
   cd backend && uv pip install -e . --dev
   
   # 前端依赖
   cd frontend && yarn install
   ```

### 进程异常退出
```bash
# 检查进程状态
ps aux | grep uvicorn  # 后端进程
ps aux | grep vite     # 前端进程

# 强制清理
./scripts/stop.sh
```

## 🌐 访问地址

启动成功后，可通过以下地址访问：

- **前端界面**：http://localhost:3000
- **后端API**：http://localhost:8000
- **API文档**：http://localhost:8000/docs
- **API Schema**：http://localhost:8000/openapi.json

## 📋 脚本功能详解

### start.sh - 主启动脚本
- 检查系统依赖（uv、yarn）
- 创建虚拟环境（后端）
- 安装项目依赖
- 启动前后端服务
- 健康检查和状态验证

### stop.sh - 停止脚本
- 优雅停止服务进程
- 清理PID文件
- 强制清理端口占用
- 支持单独停止前端或后端

### status.sh - 状态检查脚本
- 显示进程运行状态
- 检查端口占用情况
- 验证服务HTTP响应
- 显示系统信息和访问地址

### restart.sh - 重启脚本
- 停止现有服务
- 重新启动服务
- 支持单独重启前端或后端

### start-frontend.sh - 前端启动脚本
- 独立启动前端服务
- 检查yarn依赖
- 安装前端依赖包
- 启动Vite开发服务器

### start-backend.sh - 后端启动脚本
- 独立启动后端服务
- 检查uv依赖
- 创建Python虚拟环境
- 启动FastAPI服务器

## 🛠️ 开发模式

### 开发环境变量
```bash
# 前端环境变量
cp frontend/.env.example frontend/.env
# 编辑 frontend/.env 设置 VITE_API_URL

# 后端环境变量
cp backend/.env.example backend/.env
# 编辑 backend/.env 设置数据库等配置
```

### 开发端口配置
- 前端：3000（可在vite.config.ts中修改）
- 后端：8000（可在启动命令中修改）

## 🔒 生产部署

生产环境建议：
1. 使用进程管理器（如pm2、systemd）
2. 配置反向代理（如nginx）
3. 设置环境变量
4. 配置日志轮转
5. 设置监控和告警

示例生产启动命令：
```bash
# 后端生产模式
uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --workers 4

# 前端生产构建
yarn build
yarn preview
```
